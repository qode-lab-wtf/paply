# Paply

Read `docs/instinct/START.md` and `docs/STAND.md` before working.
Follow the confirmed plan in `docs/PLAN.md` and decisions in `docs/ENTSCHEIDUNGEN.md`.
Keep dictation unchanged. Do not install or release the new meeting pipeline until its
recorded quality gates pass. Never commit recordings, transcripts, model files,
credentials or private benchmark output. Use ignored `work/` for local experiments.
Run relevant tests before committing. Commit explicit task-related paths only.
