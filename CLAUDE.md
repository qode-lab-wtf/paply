# Paply

Start with `docs/instinct/START.md` and `AGENTS.md`. The canonical current status is
`docs/STAND.md`; use the same plan, decisions and validation gates as Codex.

For the 2026-09-21 takeover, read `docs/UEBERGABE-CLAUDE.md` before changing code.
Use the current worktree `/Users/allanha/Vibe/paply-local-meetings` on
`codex/local-meeting-quality`; `paply-main` is the older main checkout.
The handoff is a snapshot, not a second status source. Keep updating `docs/STAND.md`.
